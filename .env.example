# ── Root .env (git-ignored) — used ONLY by the migration scripts ─────────────
# Paste your Supabase DB connection string and replace [YOUR-PASSWORD] with the
# database password you set when creating the project.
#
# If you're on an IPv4-only network and the direct host fails, use the
# "Session pooler" string from Supabase → Project Settings → Database.
DATABASE_URL=postgresql://postgres:6Vu7PRvigLIHx8HU@db.lzkokyjkvxqkamfyzxmw.supabase.co:5432/postgres
